{
  "devCommand": "fastapi run --workers 4 app/__init__.py --host 0.0.0.0 --port 8000",
  "builds": [
    {
      "src": "app/__init__.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/__init__.py"
    }
  ]
}
